{
  "$schema": "https://raw.githubusercontent.com/qiu8310/minapp/master/schema/component.json",
  "component": true,
  "minapp": {
    "component": {
      "desc": [
        "这个组件是做什么的？",
        "支持 **markdown** 的语法"
      ],
      "attrs": [
        {
          "name": "attr1",
          "type": {
            "name": "string"
          },
          "desc": [
            "这里是这个属性的描述",
            "建议每个属性都加上描述，好处有两点：1. 可以通过 `minapp attrs` 命令将这些描述提取到当前目录中的 cjson 文件内2. 用了 minapp-vscode 的插件了的话，引用了此组件的页面可以获取到实时的提醒"
          ]
        }
      ]
    }
  }
}